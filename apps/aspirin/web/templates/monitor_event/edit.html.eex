<h2>Edit monitor event</h2>

<%= render "form.html", changeset: @changeset,
                        action: monitor_event_path(@conn, :update, @monitor_event) %>

<%= link "Back", to: monitor_event_path(@conn, :index) %>
