<h2>New monitor event</h2>

<%= render "form.html", changeset: @changeset,
                        action: monitor_event_path(@conn, :create) %>

<%= link "Back", to: monitor_event_path(@conn, :index) %>
